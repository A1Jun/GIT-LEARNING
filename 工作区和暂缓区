-----工作区(Working Directory)-----

电脑里能看见的目录，比如我的learngit文件夹就是一个工作区

-----版本库（Reposutory）-----

工作区有一个隐藏目录.git, 这个不算工作区，而是Git的版本库。

版本库中包括：stage（或者index）的暂缓区，还有第一个分支master, 以及指向master的一个指针叫HEAD。

--git add命令就是把工作区的文件提交到暂缓区
--然后用git commit -m <> 就把暂缓区的文件提交到版本库里，此时暂缓区没有文件。并且工作区是干净的。

